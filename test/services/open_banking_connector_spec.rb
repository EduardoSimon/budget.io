require "test_helper"

class OpenBankingConnectorTest < ActiveSupport::TestCase
  test "it starts an auth session from the provider" do
    # OpenBankingConnector.stub :new, MockClient.new do
    #   auth_session = OpenBankingConnector.new.init_auth_session(institution_id: "institution_id", internal_user_id: 1, redirect_url: "redirect_url")

    #   assert_equals auth_session.
    # end
  end
end
