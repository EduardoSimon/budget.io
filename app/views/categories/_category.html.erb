<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>
  <p>
    <strong>Assigned amount:</strong>
    <%= humanized_money_with_symbol category.assigned_amount %>
    <strong>Target amount:</strong>
    <%= category.target_amount %>
    <p>
      <%= link_to "Edit this category", edit_budget_category_path(@budget,category) %>
    </div>
